import defaultVert from './default.vert';
import defaultFrag from './default.frag';


export const shaderProgramSources = {
    default: {
        vertex: defaultVert,
        fragment: defaultFrag,
    },
};